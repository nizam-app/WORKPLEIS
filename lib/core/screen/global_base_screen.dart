import 'package:flutter/material.dart';

class GlobalBaseScreen extends StatelessWidget {
  const GlobalBaseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}
